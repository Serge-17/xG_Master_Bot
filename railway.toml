[build]
builder = "dockerfile"

[deploy]
startCommand = "python -m uvicorn webapp:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
