# Telegram Bot (python-telegram-bot v21 — async native)
python-telegram-bot[ext]==21.5

# FastAPI + uvicorn — entry point для HuggingFace Space (port 7860)
fastapi==0.111.0
uvicorn[standard]==0.30.1

# БД — PostgreSQL (Neon) через SQLAlchemy async
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
# SQLite fallback (локальная разработка / пустой DATABASE_URL)
aiosqlite==0.20.0

# HTTP клиент для внешних API (Gemini, football-data, the-odds-api)
aiohttp==3.9.5
beautifulsoup4==4.12.3

# Планировщик — ежедневный Матч дня
apscheduler==3.10.4

# Математика (Poisson + fair odds)
numpy==1.26.4
scipy==1.13.1
